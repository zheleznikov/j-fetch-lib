/**
 * Таймаут для промисов.
 *
 * Что делает:
 * оборачивает promise
 * отменяет через AbortController (если передан signal)
 *
 * Зачем:
 * Контроль зависших операций.
 */

export function timeout() {}