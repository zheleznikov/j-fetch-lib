/**
 * Кеширование async-функций.
 *
 * Что делает:
 *
 * кеширует результат async-вызова
 * поддерживает TTL
 * может дедуплицировать одновременные вызовы
 *
 * Зачем:
 * По сути — «умный кеш промисов», полезен сам по себе.
 */

export function memoizeAsync() {}