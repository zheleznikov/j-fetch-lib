/**
 * Универсальный retry с backoff.
 *
 * - повторяет async-функцию
 * - поддерживает delay / backoff / jitter
 * - пробрасывает ошибку после исчерпания попыток
 *
 * Зачем:
 * Переиспользуемая логика для fetch-клиента и не только.
 */

export function retry() {

}