/**
 * Генерация стабильного ключа запроса.
 * - собирает key из method + url + body
 * - гарантирует, что у одинаковых запросов будет одинаковый ключ
 *
 * Зачем:
 * Критично для:
 * - кеша
 * - dedupe in-flight запросов
 */