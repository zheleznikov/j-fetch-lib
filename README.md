# J Fetch Client (monorepo)

Packages:
- `@zhele/fetch-client` — cache + TTL + in-flight dedupe + retry/timeout + interceptors
- `@yzhele/async-utils` — retry/timeout/queue/memoize helpers

Demo app is in `apps/demo`.
